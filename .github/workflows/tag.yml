name: Tag and Publish

on:
  push:
    branches: [main]

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      should_release: ${{ steps.version.outputs.should_release }}
      version: ${{ steps.version.outputs.current }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 2

      - name: Check version status
        id: version
        run: |
          CURRENT_VERSION=$(grep -m1 'version = ' pyproject.toml | cut -d'"' -f2)
          echo "current=$CURRENT_VERSION" >> $GITHUB_OUTPUT

          if curl -s "https://pypi.org/pypi/django-formwork/$CURRENT_VERSION/json" | grep -q '"version"'; then
            echo "should_release=false" >> $GITHUB_OUTPUT
            echo "Version $CURRENT_VERSION already on PyPI — nothing to do"
            exit 0
          fi

          # Tag-exists is treated as "do nothing"; a broken tag must be deleted
          # manually before a re-release. Auto-retrying on an existing tag is how
          # we got stuck publishing the same broken commit repeatedly.
          git fetch --tags
          if git rev-parse "v$CURRENT_VERSION" >/dev/null 2>&1; then
            echo "should_release=false" >> $GITHUB_OUTPUT
            echo "Tag v$CURRENT_VERSION already exists — delete it manually to re-release"
            exit 0
          fi

          echo "should_release=true" >> $GITHUB_OUTPUT
          echo "Will release v$CURRENT_VERSION after tests pass"

  test:
    needs: check-version
    if: needs.check-version.outputs.should_release == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install 3.14

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: "22"

      - name: Install Python dependencies
        run: uv sync --group dev

      - name: Install Node dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: uv run playwright install --with-deps chromium

      - name: Generate iconx CSS
        env:
          DJANGO_SETTINGS_MODULE: settings.base
          PYTHONPATH: tests
        run: uv run python -m django formwork install

      - name: Build CSS bundle for e2e
        run: npx @tailwindcss/cli -i tests/e2e/app.css -o django_formwork/static/formwork/formwork-dist.css

      - name: Run ruff check
        run: uv run ruff check

      - name: Run ruff format check
        run: uv run ruff format --check

      - name: Run mypy check
        run: uv run mypy django_formwork/

      - name: Run ty check
        run: uv run ty check django_formwork/

      - name: Run tests
        run: uv run pytest tests/ -m "not screenshot and not benchmark" -n auto

  tag-and-trigger:
    needs: [check-version, test]
    if: needs.check-version.outputs.should_release == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    env:
      VERSION: ${{ needs.check-version.outputs.version }}
    steps:
      - uses: actions/checkout@v7

      - name: Create and push tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "v$VERSION"
          git push origin "v$VERSION"

      # GITHUB_TOKEN-pushed tags don't trigger downstream workflows — explicit
      # dispatch is required.
      - name: Trigger publish workflow
        run: gh workflow run publish.yml -f version=$VERSION -R ${{ github.repository }}
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Trigger docs workflow for new tag
        run: gh workflow run docs.yml --ref "v$VERSION" -R ${{ github.repository }}
        env:
          GH_TOKEN: ${{ github.token }}
