// Alpine.data component for the formwork PasswordReveal widget.
// Loaded as an ES module via Media.js or imported by formwork.js.

document.addEventListener("alpine:init", () => {
  Alpine.data("formworkPasswordReveal", () => ({
    show: false,
  }));
});
